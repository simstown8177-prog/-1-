
# 스토리북 운영 시스템

GitHub 업로드 → Vercel 배포용 프로젝트입니다.

## 실행

npm install
npm run dev

## GitHub 업로드

git init
git add .
git commit -m "storybook system"
git branch -M main
git remote add origin YOUR_GITHUB_REPO
git push -u origin main

## Vercel 배포

vercel.com 접속 → Import Project → GitHub → Deploy
